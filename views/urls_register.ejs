<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/static/css/body.css" />
    <title>Register for TinyApp</title>
  </head>

  <body>
    <%- include('partials/_header', {user: user}) %>
    <main class="main-login" style="margin: 1em">
      <h3>New to TinyApp? Register here!</h3>
      <center>
        <form class="form-inline" action="/register" method="POST">
          <div class="form-group mb-2">
            <label for="registerEmail">Enter your email address here:</label>
            <input
              class="form-control"
              type="email"
              name="email"
              placeholder="user@email.com"
              style="width: 300px; margin: 1em"
            />
            <br />
            <label for="registerPassword">Enter your password here:</label>
            <input
              class="form-control"
              type="password"
              name="password"
              style="width: 300px; margin: 1em"
            />
            <br />
            <button type="submit" class="btn btn-primary">Register</button>
          </div>
        </form>
      </center>
      <br />
      <!-- <p>
        <a href="/urls">Go Home</a>
      </p> -->
      <img class="flowers" src="/static/flowers.png" />
    </main>
  </body>
</html>
