<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/static/css/body.css" />
    <title>Login to TinyApp</title>
  </head>

  <body>
    <%- include('partials/_header', {user: user}) %>
    <main class="main-login" style="margin: 1em">
      <h3>Login to TinyApp :)</h3>
      <form class="form-inline" action="/login" method="POST">
        <div class="form-group mb-2">
          <label for="loginEmail">Enter your email address here:</label>
          <input
            class="form-control"
            type="email"
            name="email"
            placeholder="user@email.com"
            style="width: 300px; margin: 1em"
          />
          <label for="loginPassword">Enter your password here:</label>
          <input
            class="form-control"
            type="password"
            name="password"
            style="width: 300px; margin: 1em"
          />
          <button type="submit" class="btn">Login</button>
        </div>
      </form>
      <img class="flowers" src="/static/flowers.png" />
    </main>
  </body>
</html>
