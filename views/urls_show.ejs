<!DOCTYPE html>
<html lang="en">
  <head>
    <title>URL - TinyApp Example</title>
    <link rel="stylesheet" href="/static/css/body.css" />
  </head>

  <body>
    <%- include('partials/_header', {user: user}) %>
    <div class="card text-center">
      <div class="card-header"></div>
      <% if (!user) { %>
      <div class="card-body">
        <p>You're not logged in! Please login to access this page.</p>
      </div>
      <% } else if (!shortURL) { %>
      <div class="card-body">
        <p>This does not belong to you. Go away!</p>
      </div>
      <% } else { %>
      <div class="card-body">
        <h5 class="card-title">TinyURL for: <%= longURL %></h5>
        <p class="card-text">
          Short URL: <a href="/u/<%= shortURL %>"><%= shortURL %></a>
        </p>
      </div>
      <div class="card-footer text-muted">
        <label>
          <h3>edit</h3>
        </label>
        <form
          class="form-inline justify-content-center"
          method="POST"
          action="/urls/<%= shortURL %>"
        >
          <input type="text" name="longURL" placeholder="http://" />
          <button type="submit" class="btn btn-info">submit</button>
        </form>
      </div>
      <% } %>
    </div>
  </body>
</html>
